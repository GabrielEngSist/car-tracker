namespace Car.Tracker.Tests;

public sealed class PlaceholderTests
{
    [Fact]
    public void Placeholder_removed_by_real_tests()
    {
        // Intentionally empty; real tests live in feature-aligned folders.
        // This exists only to keep the project compiling if the test runner filters folders.
        Assert.True(true);
    }
}
