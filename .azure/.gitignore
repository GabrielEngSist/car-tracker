# azd creates per-environment folders here that contain local state and may
# contain secrets — those MUST NOT be committed. The deployment plan is the
# one exception: it's the source of truth read by azure-validate / azure-deploy.
*
!.gitignore
!deployment-plan.md
